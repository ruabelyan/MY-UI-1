import { buttonDefaultPresets } from '@core/styles/presets';
import { PresetThemeConfig } from './types';

const presetDefaultValues: PresetThemeConfig = {
  Button: buttonDefaultPresets,
};

export default presetDefaultValues;
